export type Assignments = {
    deadline: number | string | Date;
    lessonId: number,
    maxScore: number,
    studentSlug: string,
    taskId: number,
    teacherComment: string,
    teacherSlug: string
}